[package]
name = "v2f-pnr"
version = "0.1.0"
edition = "2021"

[dependencies]
v2f-core = { path = "../v2f-core" }
v2f-db = { path = "../v2f-db" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"

[dev-dependencies]
v2f-synth = { path = "../v2f-synth" }
